# How-To: <Task Name>

## Goal

<In 1–2 sentences, what will the reader accomplish?>

## Audience

- <Who is this for? (end users / integrators / internal devs)>

## Prerequisites

- <Required accounts/access>
- <Required environment variables/config>
- <Required versions/constraints>

## Steps

1. <Step name>

   ```bash
   <command>
   ```

   Expected output:

   ```
   <what “success” looks like>
   ```

2. <Next step>

## Verification

- <How the reader confirms it worked (command output, UI state, API response, etc.)>

## Troubleshooting

### Symptom: <what the user sees>

- Likely cause: <most common real cause>
- Fast check:
  ```bash
  <command>
  ```
- Fix: <steps>

## Next steps

- <Where to go next: deeper guide, concept doc, API reference>

## Evidence

- Code: `path/to/file.ext:line`
- Tests: `path/to/test.ext:line`
- Spec/schema: `path/to/spec.ext:line` (or URL if applicable)
- Tool output: <link/ref to build logs, CLI output, etc.>
